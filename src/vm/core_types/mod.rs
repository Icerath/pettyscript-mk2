use crate::vm::prelude::*;

impl CanObj for i64 {
    fn get_item(obj: &Obj<PtyPtr>, key: &str) -> Obj<PtyPtr> {
        todo!()
    }
}

impl CanObj for String {
    fn get_item(obj: &Obj<PtyPtr>, key: &str) -> Obj<PtyPtr> {
        todo!()
    }
}
